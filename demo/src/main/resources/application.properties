# JDBC URL: host, port, database, SSL settings
spring.datasource.url=jdbc:mysql://tramway.proxy.rlwy.net:53682/cartrizo?useSSL=false&allowPublicKeyRetrieval=true

# Database credentials
spring.datasource.username=caruser
spring.datasource.password=carpassword123

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server port
server.port=5000
